export interface Genre {
    id: string;
    name: string;
    created_at: string // string date
}

export interface Language {
    id: string;
    name: string;
    created_at: string // string date
}